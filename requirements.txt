fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic-settings>=2.4
cryptography>=43.0
httpx>=0.27
PyYAML>=6.0
PyJWT>=2.8
alembic>=1.13
python-multipart>=0.0.9

# 선택 의존성 — 없는 환경에서도 서버는 기동되며 해당 기능만 비활성화됩니다.
# docker>=7.0          # 1차(small) 런타임
# kubernetes>=30.0     # 2차(enterprise) 런타임에서 직접 apply할 때
# psutil>=6.0          # 시스템 모니터링
# nvidia-ml-py>=12.0   # GPU/VRAM 모니터링
