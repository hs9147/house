# 저장소·설정 영속 볼륨. StorageClass는 클러스터에 맞게 조정할 것 — 미지정 시 기본 클래스 사용.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-data
  namespace: gitea
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
  # storageClassName: <클러스터의 StorageClass>
